//to go to the repository 
cd //copy and past the path
git init //"start the repo"
touch //make new folder 

to commit the change in any file we need to first
git status
git add "name of the file" you want the changes.
